# Requirements for Depth Anything 3 Examples
# Install with: pip install -r requirements.txt

# Core dependencies (should be installed with main package)
# torch>=2.0.0
# torchvision
# huggingface_hub
# numpy
# pillow
# opencv-python

# Additional dependencies for examples
matplotlib>=3.5.0        # For plotting and visualization
tqdm>=4.65.0             # For progress bars
opencv-python>=4.8.0     # For video processing (if not already installed)

# Optional but recommended
ipython                  # For interactive debugging
jupyter                  # For running notebooks
